{
  "name": "memento-design-pattern",
  "version": "1.0.0",
  "description": "A TypeScript implementation of the Memento behavioral design pattern using a text editor with undo/redo functionality",
  "main": "dist/demo.js",
  "scripts": {
    "dev": "ts-node src/demo.ts",
    "demo": "ts-node src/demo.ts --demo",
    "build": "tsc",
    "start": "node dist/demo.js",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "design-patterns",
    "memento-pattern",
    "behavioral-pattern",
    "undo-redo",
    "text-editor"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
